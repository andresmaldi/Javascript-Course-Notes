// Manipulating CSS Styles
// It is possible to manipulate CSS styles with the DOM
// To access the CSS element we use .style followed by the property you wish to change
// example:

document.querySelector("body").style.backgroundColor = "green";

// The change you wish to implement must be inside a string, since the DOM won't be able to read it otherwise
